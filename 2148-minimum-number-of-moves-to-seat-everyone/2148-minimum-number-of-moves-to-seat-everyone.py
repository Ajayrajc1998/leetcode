class Solution(object):
    def minMovesToSeat(self, seats, students):
        """
        :type seats: List[int]
        :type students: List[int]
        :rtype: int
        """
        seats.sort()
        students.sort()
        result=[abs(seats[i]-students[i]) for i in range(len(seats))]
        return sum(result)